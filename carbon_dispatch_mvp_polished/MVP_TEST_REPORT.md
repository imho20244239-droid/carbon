# 碳算智调 MVP 测试报告（精修版）

## 1. 核心调度回归

产品化 `core/solver.py` 在72批基准任务上重新求解，复现原公开校准协同结果：

- 现金成本：585.8226 元
- 调度碳代理：243.6320 kgCO₂
- 位置法碳排：619.2893 kgCO₂
- 市场法碳排：263.5078 kgCO₂
- 任务完成率：100%
- SLA满足率：100%
- 绿电占比：71.6538%

`python smoke_test.py` 已通过。

## 2. 精修版新增交互后端检查

### 9点碳权重扫描

已实际调用统一 `solve_dispatch()` 对以下权重重新求解：

`0.00 / 0.05 / 0.10 / 0.15 / 0.25 / 0.40 / 0.60 / 0.80 / 1.00`

全部任务完成率与SLA保持100%。随着影子碳价提高，解从成本优先端逐步向低调度碳端移动。该扫描用于页面形成“权重扫描候选前沿”，不是另起一套模型。

### 快速压力场景

精修版预设均通过统一压力测试接口重新求解：

- 高碳价：1.00元/kg；
- 绿电紧张：0.40×；
- 绿电充足：1.50×；
- 高波动：3.00×；
- 需求冲击：1.30×。

需求1.30×场景重新求解得到约92.3865%的任务完成率，因此容量风险提示来自当前模型结果，不是固定文案。

## 3. validation_suite_v4 验证证据

本版接入：

- 5组消融原始结果；
- 50组 Monte Carlo 明细与汇总；
- 4/8/12/16/20节点规模测试；
- 原验证脚本、关键结论与运行日志。

重新运行验证套件后，消融和Monte Carlo业务指标可复现；单次求解时间受机器状态影响。4/8/12节点规模结果稳定；16/20节点由于5秒时限，不同机器/负载下可能在时限前获得不同 incumbent，因此MIP Gap、目标值和求解时间允许小幅波动。

原始上传记录中的20节点结果为：约5.084s、MIP Gap=0.005559（比例），即约0.556%，任务完成率与SLA均100%。

`python validation_evidence_test.py` 已通过。

## 4. 静态检查

- `app.py`、`core/*.py`、`pages/*.py`、两套测试脚本均通过 `py_compile`；
- 核心MILP回归通过；
- 验证证据完整性检查通过；
- 新增权重扫描和压力预设均已直接调用后端求解完成数值检查。

当前执行环境仍未安装 Streamlit，因此无法在此环境完成浏览器级截图验收；最终浏览器视觉应在参赛电脑启动后按 `DEMO_GUIDE.md` 做一次全页面巡检。
