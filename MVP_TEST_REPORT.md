# 碳算智调 MVP 测试报告

## 1. 核心调度回归

产品化 `core/solver.py` 在72批基准任务上重新求解，复现原公开校准协同结果：

- 现金成本：585.8226 元
- 调度碳代理：243.6320 kgCO₂
- 位置法碳排：619.2893 kgCO₂
- 市场法碳排：263.5078 kgCO₂
- 任务完成率：100%
- SLA满足率：100%
- 绿电占比：71.6538%

`python smoke_test.py` 已通过。

## 2. 压力测试回归

组合压力测试引擎已对单因素切片进行回归：碳价=1.0、绿电×1.5、绿电波动=3.0与原 `stress_bp_summary.csv` 一致；需求×1.30重新求解得到约92.3865%的任务完成率，因此容量风险提示来自当前模型结果，不是固定文案。

## 3. validation_suite_v4 补充验证

本版已接入：

- 5组消融原始结果；
- 50组 Monte Carlo 明细与汇总；
- 4/8/12/16/20节点规模测试；
- 原验证脚本、关键结论与运行日志。

重新运行 `legacy/carbon_compute_validation_suite_v4.py` 后：

- 消融业务结果可精确复现；
- Monte Carlo 的需求、容量、电价、绿电随机扰动及成本/碳业务结果可复现，单次求解时间因机器状态存在毫秒级差异；
- 4/8/12节点规模结果稳定；16/20节点由于5秒时限，求解器在不同机器/负载下可能在时限到达前找到不同 incumbent，因此MIP Gap、目标值和求解时间允许小幅波动。这正是时间限制 MILP 的正常行为，不应使用逐位一致性作为验证标准。

原始上传记录中的20节点结果为：5.084s、MIP Gap=0.005559（比例），即约0.556%，任务完成率与SLA均100%。

`python validation_evidence_test.py` 用于检查证据完整性和稳定业务结论，不对时间受限档位做错误的逐位复现要求。

## 4. 静态检查

全部 Python 文件进行 `py_compile` 语法检查。由于当前执行环境未安装 Streamlit，无法在本环境完成浏览器级 UI 启动截图测试；后端求解、数据加载与验证证据测试均已实际执行。
